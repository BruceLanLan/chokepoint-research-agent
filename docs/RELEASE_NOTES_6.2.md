# Release notes — v6.2.0

Multi-symbol quote batch + remote plugin design (docs only).

```bash
python main.py multi-quotes NVDA,AVGO,TSM
```

See [REMOTE_PLUGIN_DESIGN.md](./REMOTE_PLUGIN_DESIGN.md).

**Research only — not investment advice.**
