# Release notes — v6.1.0

Research desk, pro dashboard, memo→pro bridge, glossary search, quote history charts.

```bash
python main.py desk --md
python main.py pro-dashboard
python main.py memo-pro some.md --modules risk_matrix,moat_score
python main.py pro-export
python main.py glossary CPO
python main.py quote-chart NVDA
```

API: `/desk`, `/pro/dashboard`, `/memo-pro`, `/pro/export`, `/glossary`, `/charts/quote-history`, `/questionnaires`, `/rubrics`

**Research only — not investment advice.**
