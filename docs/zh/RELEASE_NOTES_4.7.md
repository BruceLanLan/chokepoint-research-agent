# 发布说明 — v4.7.0

本地扩展 marketplace、live 队列费用门禁、行情能力说明、CI 冒烟。

```bash
python main.py marketplace
python main.py queue --estimate-live
python main.py queue --run 1   # 默认 mock
# live 需显式：--i-accept-live-costs
```

**仅供研究学习，不构成投资建议。**
