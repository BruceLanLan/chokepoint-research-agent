# 发布说明 — v4.3.0

本地 SQLite FTS 研报索引 + 研究队列（先规划、后调用模型，省额度）。

```bash
python main.py index-memos --q CPO
python main.py queue --from-map cpo_ai_interconnect
python main.py queue --summary
```

**仅供研究学习，不构成投资建议。**
