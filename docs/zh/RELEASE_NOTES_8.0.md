# 发布说明 — v8.0.0

**专业 UI/UX + 包结构硬化**

## 亮点

1. **工作台 UI** — 深色专业壳层、Desk 优先导航、12 个标签页、中/英切换、研报行内动作（查看/清单/评分/导出包）、SSE 研究流、Toast 反馈。
2. **API 包** — `src/api/`（`factory`、`deps`、按域路由）；入口仍为 `src.api:app`。
3. **CLI 包** — `src/cli/` Typer 分组；`main.py` 薄入口。
4. **垂直研究包深化** — CPO / HBM / 供电散热 / 机器人执行器 / 特种材料：物理节点、杀逻辑、证据清单。

## 兼容性

- CLI 命令名不变；HTTP 路由不变；离线测试 121 通过。

仅供研究学习，不构成投资建议。
