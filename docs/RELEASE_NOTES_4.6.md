# Release notes — v4.6.0

Publish checklist gate + offline eval history trends.

中文 → [zh/RELEASE_NOTES_4.6.md](./zh/RELEASE_NOTES_4.6.md)

```bash
python main.py checklist some_memo.md
python main.py eval-record
python main.py eval-trend
python main.py doctor
```

API: `GET /checklist/{name}`, `POST /eval/record`, `GET /eval/trend`

**Research only — not investment advice.**
