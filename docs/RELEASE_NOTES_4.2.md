# Release notes — v4.2.0

Knowledge maps as first-class research seeds, cost/quality dashboard, richer briefs.

中文 → [zh/RELEASE_NOTES_4.2.md](./zh/RELEASE_NOTES_4.2.md)

```bash
python main.py maps
python main.py maps cpo_ai_interconnect --mermaid
python main.py maps cpo_ai_interconnect --seed
python main.py dashboard
python main.py eval
```

API: `GET /maps`, `GET /maps/{id}?seed=true|mermaid=true`, `GET /dashboard`

**Research only — not investment advice.**
