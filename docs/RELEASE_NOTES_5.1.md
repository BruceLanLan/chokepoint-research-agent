# Release notes — v5.1.0

Memo professional grade + watchlist quote cache refresh.

```bash
python main.py grade-memo some_memo.md
python main.py quotes-cache --from-watchlist
```

API: `GET /grade/{name}`, `POST /quotes/cache/refresh` with `{"from_watchlist":true}`

**Research only — not investment advice.**
