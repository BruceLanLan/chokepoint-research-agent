# Demo stack — do not expose to the public internet without auth.
# Usage:
#   cp .env.example .env   # fill secrets
#   docker compose up --build

services:
  agent:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./reports:/app/reports
    restart: unless-stopped
